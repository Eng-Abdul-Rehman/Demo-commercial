import { palette } from '../Palette';

const MuiTooltip = () => ({
  MuiTooltip: {
    styleOverrides: {
      tooltip: {
        backgroundColor: palette.storm[100],
      },
    },
  },
});

export default MuiTooltip;
